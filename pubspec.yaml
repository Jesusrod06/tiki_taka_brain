name: tiki_taka_brain
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Librerías necesarias para el proyecto
  shared_preferences: ^2.2.2
  animate_do: ^3.3.4
  provider: ^6.1.1
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # Si vas a usar imágenes, descomenta las líneas de abajo:
  # assets:
  #   - assets/images/